tosca_definitions_version: 2.0
imports:
- url: node_types.yaml
service_template:
  node_templates:
    my_application:
      type: WebApplication
      count: 3
      requirements:
        - host: 
            node: [ tomcat_server, $node_index ]